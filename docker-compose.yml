version: '3.4'

# TODO : replace olivierdalang repository by pacificcommunity



# Common Django template for Geonode, Celery and Celerycam services below
x-common-django:
  &default-common-django
  image: olivierdalang/spcgeonode:django-0.0.9
  build: ./django/
  environment:
    - GEOSERVER_PUBLIC_LOCATION=/geoserver/ # we use a relative URL since we want to keep current scheme/host
    - ALLOWED_HOSTS=${WAN_HOST},${LAN_HOST},nginx # we need nginx to allow calls to Geonode OAuth API from Geoserver # TODO : remove this if we don't use OAuth2
    - ADMIN_EMAIL=${ADMIN_EMAIL}
    - DJANGO_SETTINGS_MODULE=spcgeonode.settings
    - DEBUG=False
    - REGISTRATION_OPEN=True
    - DATABASE_URL=postgres://postgres:postgres@postgres:5432/postgres
    - BROKER_URL=amqp://guest:guest@rabbitmq:5672/
    - STATIC_ROOT=/spcgeonode-static/
    - MEDIA_ROOT=/spcgeonode-media/
    - STATIC_URL=/static/
    - MEDIA_URL=/media/
    - GEOSERVER_LOCATION=http://nginx/geoserver/
    - C_FORCE_ROOT=1 # TODO : understand that (is it a security concern ?)
    - TIME_ZONE=Pacific/Fiji
  volumes:
    - static:/spcgeonode-static/
    - media:/spcgeonode-media/
  secrets:
    - admin_username
    - admin_password
  deploy:
    replicas: 1
    restart_policy:
      condition: on-failure
    

services:

  # Our custom django application. It includes Geonode.
  django:
    << : *default-common-django
    entrypoint: ["/spcgeonode/docker-entrypoint.sh"]
    command: ["uwsgi", "--chdir=/spcgeonode", "--module=spcgeonode.wsgi", "--socket=:8000", "--processes=5"]
    
  # Celery worker that executes celery tasks created by Django.
  celery:
    << : *default-common-django
    command: 'python manage.py celery worker --app=geonode.celery_app:app -B -E -l INFO'

  # Celery camera that monitors celery tasks and populate the djcelery django admin interface
  celerycam:
    << : *default-common-django
    command: 'python manage.py celerycam'

  # PostGIS database.
  postgres:
    image: mdillon/postgis:9.6-alpine
    volumes:
      - database:/var/lib/postgresql/data/ 
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

  # TODO : Enable this
  # elasticsearch:
  #   image: elasticsearch:5.6

  # Vanilla RabbitMQ service. This is needed by celery
  rabbitmq:
    image: rabbitmq:3.7 
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
  
  # Nginx is serving django static and media files and proxies to django and geonode
  nginx:
    image: olivierdalang/spcgeonode:nginx-0.0.9
    build: ./nginx/
    environment:
      - WAN_HOST=${WAN_HOST}
      - LAN_HOST=${LAN_HOST}
      - RESOLVER=127.0.0.11 # SET THIS TO 169.254.169.250 FOR RANCHER
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - static:/spcgeonode-static/
      - media:/spcgeonode-media/
      - certbot:/spcgeonode-certbot/
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

  # Geoserver backend
  geoserver:
    image: olivierdalang/spcgeonode:geoserver-0.0.9
    build: ./geoserver/
    environment:
     - GEOSERVER_HOME=/geoserver-2.12.1
     - GEOSERVER_DATA_DIR=/spcgeonode-geodatadir
    volumes:
      - geodatadir:/spcgeonode-geodatadir/
    secrets:
      - admin_username
      - admin_password 
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

  # Gets and installs letsencrypt certificates
  letsencrypt:
    image: olivierdalang/spcgeonode:letsencrypt-0.0.9
    build: ./letsencrypt/
    environment:
      - WAN_HOST=${WAN_HOST}
      - LAN_HOST=${LAN_HOST}
      - ADMIN_EMAIL=${ADMIN_EMAIL}
      - LETSENCRYPT_MODE=${LETSENCRYPT_MODE}
    volumes:
      - certbot:/spcgeonode-certbot/
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 60s
 
  # TODO : add a cron job to backup database
  
  pgdumper:
    image: olivierdalang/spcgeonode:pgdumper-latest
    build: ./pgdumper/
    volumes:
      - pgdumps:/spcgeonode-pgdumps/ 

  syncthing:
    image: olivierdalang/spcgeonode:syncthing-latest
    build: ./syncthing/
    environment:
      - REMOTE_SYNCTHING_MACHINE_ID=${REMOTE_SYNCTHING_MACHINE_ID}
    secrets:
      - admin_username
      - admin_password
    ports:
      - "8384:8384" # TODO : put this behind nginx too ?
      - "22000:22000"
    volumes:
      - pgdumps:/spcgeonode-pgdumps/
      - media:/spcgeonode-media/
      - geodatadir:/spcgeonode-geodatadir/

volumes:
  static:
  media:
  database:
  geodatadir:
  certbot:
  pgdumps:

secrets:
  admin_username:
    external: true
  admin_password:
    external: true
